---
- disrupt_action:
    - name: Kill services on VM start
      listener:
        re: mutaki
        log: /tmp/pinglocal
        host: localhost
      trigger:
        - action:
            name: restart_service
            params: syslog
            target_host: localhost
            wait: 3
            timeout: 3
